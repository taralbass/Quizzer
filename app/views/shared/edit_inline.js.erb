$('#<%= dom_id(target, attr) %>')
  .closest('.inline_managed_attribute')
  .find('.restore')
    .hide()
  .end()
  .find('.inline_displayed')
    .replaceWith('<%= escape_javascript render('shared/inline_editable', :target => target, :attr => attr, :partial => partial) %>');
